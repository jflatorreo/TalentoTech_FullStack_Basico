function generarHistoria(nombre,lugar,objeto){
    /* Tu código para generar la historia... Ej..
    
    let historia= "Estaba " + nombre + " en" + lugar + " jugando con un "+objeto;
    
    o así:

    let historia = `Estaba ${nombre} en ${lugar} jugando con un ${objeto}`
    */

    return historia
}

let nombre="maria";
let lugar="bogota";
let objeto="dados";

historia = generarHistoria(nombre,lugar,objeto);

console.log(historia)